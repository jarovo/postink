apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postink.fullname" . }}-test-connection"
  labels:
    {{- include "postink.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postink.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
