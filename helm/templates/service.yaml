apiVersion: v1
kind: Service
metadata:
  name: {{ include "postink.fullname" . }}
  labels:
    {{- include "postink.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: smtp
      protocol: TCP
      name: smtp
  selector:
    {{- include "postink.selectorLabels" . | nindent 4 }}
